The simplest web server. This directory is at the root of the web server, and sub directories may assume the following functions\+: \begin{DoxyVerb}data            for database related
content         where the files live
    siteicons       icons for this site
    media           images, media
    static          files that may be presented as is
    proj*           python and html / css files that make up the site
\end{DoxyVerb}
 To create a {\bfseries{new project}}, add a new directory that starts with \char`\"{}proj\char`\"{} (like \char`\"{}proj-\/000\char`\"{}) and populate the files. At least one python file is needed.

To create a {\bfseries{new page}}, add a new python file, and call the URL registration function(s) within. The server redirects the url to the function you specify.

add\+\_\+one\+\_\+url(\char`\"{}/\char`\"{}, got\+\_\+index, \char`\"{}index.\+html\char`\"{})

To create a new \textquotesingle{}macro\textquotesingle{} use the add macro function with the macro name and the name of the function that is executed by the macro. The macro will be replaced by the return value of the function. The macro is created by surrounding braces with a space. Like\+: \{ mymacro \} The macro regex is \textquotesingle{}\{ .$\ast$? \}\textquotesingle{} \mbox{[}the \textquotesingle{}?\textquotesingle{} is for non greedy wild card)

add\+\_\+one\+\_\+func(\char`\"{}mymacro\char`\"{}, my\+\_\+img\+\_\+func)

Builtin macros\+:

\{ image nnn \} -- Put an image tag nnn in the output \{ image nnn www \} -- Put an image tag in the output, resize width to requested \{ image nnn www hhh \} -- Put an image tag in the output, resize to width / height

The first two forms of the \{ image \} function will preserve the image\textquotesingle{}s aspect ratio.\hypertarget{md_doc3_wsgi_main_autotoc_md1}{}\doxysection{Functions}\label{md_doc3_wsgi_main_autotoc_md1}
{\ttfamily application(environ, respond)} \+: WSGI main entry point. The web server (like apache) will call this.

{\ttfamily tracex(xstr)} \+: Trace current fault. This was crafted, so the apache run time can access it without any includes.

{\ttfamily \mbox{\hyperlink{namespacewsgi__main_ac700c16970a4fe41bfefb70f0444216e}{xhelp()}}} \+: ~\newline


{\ttfamily \mbox{\hyperlink{namespacewsgi__main_a575120d1726ea720b5b84b24e9721310}{xversion()}}} \+: ~\newline
\hypertarget{md_doc3_wsgi_main_autotoc_md2}{}\doxysection{Classes}\label{md_doc3_wsgi_main_autotoc_md2}
{\ttfamily Myconf()} \+: Simplified config for propagating command line to runtime

{\ttfamily comline()} \+: Command line action defines \begin{DoxyVerb}### Class variables

`CLEAR_CONFIG`
:

`SHOW_CONFIG`
:

`SHOW_TIMING`
:

`USE_STDOUT`
:
\end{DoxyVerb}
 {\ttfamily x\+Web\+Server(environ, respond)} \+: This class has all the info we would need for page generation. Add urls and functions to complete the reply. The return value of the process\+\_\+request function is the output to the client.

Decorate the class instance with data from the environment\hypertarget{md_doc3_wsgi_main_autotoc_md3}{}\doxysubsection{Methods}\label{md_doc3_wsgi_main_autotoc_md3}
{\ttfamily parse\+\_\+instance(self, environ, respond)} \+:

{\ttfamily process\+\_\+request(self, request, respond)} \+: This executes the request, after the main initializer parsed everything 